services:
  backend:
    build: 
      context: ./backend
    ports:
      - "8080:8888"
    # Ensure hot-reloading isn't expected here; this builds the static binary

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3333"
    depends_on:
      - backend