services:
  backend:
    build: 
      context: ./backend
    ports:
      - "8080:8080"
    # Ensure hot-reloading isn't expected here; this builds the static binary

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend